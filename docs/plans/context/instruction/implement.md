auth-architecture-redesign.mdに従って実装してください。
auth-architecture-redesign.mdを一次情報として参照してください。不足情報の補完が必要な場合に限り、Previous Responseや会話履歴を補助的に参照して構いません（Previous Responseは提供されない場合があります）。

**重要**: 実装と同時に単体テストを追加してください。

- 新規作成したクラス・関数には単体テストを追加
- 既存コードを変更した場合は該当するテストを更新
- テストファイルの配置: プロジェクトの規約に従う
- ビルド/静的検証は必須。プロジェクト定義のコマンドがある場合はそれを優先し、Python バックエンドでは `python -m compileall backend/app backend/tests` を最低限実行して構文エラーがないことを確認
- テスト実行は必須。静的検証成功後、必ずプロジェクト定義のテストコマンド（Python バックエンドでは `pytest`）を実行して結果を確認
- ファイル名・設定キー名などの契約文字列を新規導入する場合は、定数として1箇所で定義すること

**Scope出力契約（実装開始時に作成）:**
`../output_contracts/coder-scope.md` に従って出力してください。

**Decisions出力契約（実装完了時、決定がある場合のみ）:**
`../output_contracts/coder-decisions.md` に従って出力してください。

**TDDフェーズからの引き継ぎ:**
`write-tests-first.md` で作成された失敗テストや未実装 import エラーは、実装対象を示す入力として扱ってください。実装フェーズでは、テストを削除・弱体化してグリーンにするのではなく、プロダクションコードを実装して失敗を解消してください。

**実装完了前の自己チェック（必須）:**
ビルドとテストを実行する前に、以下を確認してください:

- 新しいパラメータ/フィールドを追加した場合、grep で呼び出し元から実際に渡されているか確認した
- `or fallback`、`.get(..., default)`、デフォルト引数を使った箇所で、フォールバックが本当に必要か確認した
- リファクタリングで置き換えたコード・エクスポートが残っていないか確認した
- タスク指示書にない機能を追加していないか確認した
- if/else で同一関数を呼び出し、引数の差異のみになっていないか確認した
- 新しいコードが既存の実装パターン（API呼び出し方式、型定義方式等）と一致しているか確認した

**必須出力（見出しを含める）**

## 作業結果

- {実施内容の要約}

## 変更内容

- {変更内容の要約}

## ビルド/静的検証結果

- {実行コマンドと結果}

## テスト結果

- {テスト実行コマンドと結果}
